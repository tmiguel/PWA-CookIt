name: Deploy PWA Cookit para Firebase Hosting

on:
  push:
    branches:
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      # ... (Steps de Checkout, Node.js, Autenticar, Instalar CLI)
      
      # Restaurar a flag --project para forçar a identificação do projeto.
      - name: Deploy Final para Firebase Hosting
        # O comando deve incluir o ID do projeto, pois o CLI falha ao ler o firebase.json
        run: firebase deploy --project cookit-83f40 --only hosting
